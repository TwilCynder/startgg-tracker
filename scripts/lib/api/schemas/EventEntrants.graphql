query EventEntrants($slug: String!) {
	event(slug: $slug){
        entrants {
            nodes {
                participants {
                    user {
                        id
                    }
                }
            }
        }
    }
}